<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AgriNova" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_green</item>
        <item name="colorSecondary">@color/light_green</item>
        <item name="colorTertiary">@color/accent_yellow</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/primary_green</item>
        <item name="android:textColor">@color/dark_text</item>
    </style>

    <style name="Theme.AgriNova" parent="Base.Theme.AgriNova">
        <item name="android:windowAnimationStyle">@style/ActivityAnimation</item>
    </style>

    <style name="ActivityAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/activity_in</item>
        <item name="android:activityOpenExitAnimation">@anim/activity_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/activity_back_in</item>
        <item name="android:activityCloseExitAnimation">@anim/activity_back_out</item>
    </style>

    <style name="CircularImageView" parent="">
        <item name="cornerSize">50%</item>
    </style>

    <style name="RoundedImageView" parent="">
        <item name="cornerSize">12dp</item>
    </style>

    <style name="RoundedCard" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">4dp</item>
    </style>

    <style name="RoundedButton" parent="Widget.Material3.Button">
        <item name="shapeAppearanceOverlay">@style/ButtonRoundedCorner</item>
    </style>

    <style name="ButtonRoundedCorner" parent="">
        <item name="cornerSize">24dp</item>
    </style>

</resources>